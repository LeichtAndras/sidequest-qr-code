# Opcionális: ide POST-olja a tracking az eseményeket (sendBeacon, JSON body).
# Ha üres, csak a gtag/dataLayer ág fut (ha be van kötve az index.html-be).
VITE_QR_TRACK_ENDPOINT=""

# Upstash Redis a swipe-adatokhoz. A Vercel Marketplace integráció injektálja
# éles környezetben; lokálisan: npx vercel env pull .env.local
UPSTASH_REDIS_REST_URL=""
UPSTASH_REDIS_REST_TOKEN=""
